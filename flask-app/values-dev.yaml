# Dev environment overrides
replicaCount: 1

image:
  tag: "dev-latest"

env:
  ENVIRONMENT: "development"

autoscaling:
  enabled: false

resources:
  requests:
    memory: "32Mi"
    cpu: "50m"
  limits:
    memory: "64Mi"
    cpu: "100m"
